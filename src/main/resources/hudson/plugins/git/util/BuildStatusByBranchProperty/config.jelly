<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
      
      <f:entry field="enabled"
               title="${%Show per-branch build status}">
        <f:textbox/>
      </f:entry>
      <f:entry field="daysToKeepBranchStatusInView"
               title="${%Hide a branch's build status if it hasn't been built in this many days}">
        <f:textbox/>
      </f:entry>
</j:jelly>
